# ============================================================
# @postfiatorg/pft-chatbot-mcp Environment Variables
# ============================================================
# Copy this file to .env and fill in BOT_SEED.
# All other values have sensible testnet defaults.
# ============================================================

# REQUIRED -- The only value a bot operator must provide.
# Provide the seed via ONE of these methods (BOT_SEED takes priority):
#
# Option A: Direct seed (simpler, fine for development)
BOT_SEED=
#
# Option B: Path to a file containing the seed (more secure for production)
# Create the file with: echo "sEdYourSeed" > ~/.pft-bot-seed && chmod 600 ~/.pft-bot-seed
# BOT_SEED_FILE=~/.pft-bot-seed

# AUTO-PROVISIONED -- Obtained automatically on first register_bot call.
# The MCP server caches this to .keystone-api-key after provisioning.
# You can also set it manually if you already have one.
# KEYSTONE_API_KEY=

# OPTIONAL -- Override testnet defaults for custom deployments.
# PFTL_RPC_URL=https://rpc.testnet.postfiat.org
# PFTL_WSS_URL="wss://ws.testnet.postfiat.org"
# IPFS_GATEWAY_URL=https://pft-ipfs-testnet-node-1.fly.dev
# KEYSTONE_GRPC_URL=keystone-grpc.postfiat.org:443

# DEVELOPMENT ONLY -- Set to "true" to disable TLS for gRPC connections.
# Only use this for local development with localhost endpoints.
# KEYSTONE_GRPC_INSECURE=true
